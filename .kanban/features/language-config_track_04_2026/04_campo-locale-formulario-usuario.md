# 04 — Campo locale en formulario de usuario

**Track:** language-config
**Proyecto:** core
**Prioridad:** medium
**Estado:** backlog

Agregar `->select('locale', ...)` en `UserFormRequest` de Landlord y ActivitiesBoard. Actualizar `UserService::update()` para persistir el campo.
