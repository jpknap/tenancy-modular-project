# 05 — Archivos de traducción es/en

**Track:** language-config
**Proyecto:** core
**Prioridad:** low
**Estado:** backlog

Publicar `php artisan lang:publish`. Crear `lang/es/` y `lang/en/` para validation, auth, pagination y strings propios de la UI (`lang/es/app.php`, `lang/en/app.php`).
