# 04 — Migration tabla audits en DB central

**Track:** audit-system
**Proyecto:** landlord
**Prioridad:** high
**Estado:** backlog

Crear migration en `database/migrations/` (raíz, DB central) para la tabla `audits`. Necesaria para trackear cambios al modelo `Tenant` que vive en la DB central. Misma estructura que la migration tenant.
