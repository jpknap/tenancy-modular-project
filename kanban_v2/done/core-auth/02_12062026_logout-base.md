# 02 — Logout base compatible multi-proyecto

**Track:** core-auth
**Proyecto:** core
**Prioridad:** high
**Estado:** done

## Qué se hizo
- `TopbarComposer` inyecta `logoutUrl` derivada del prefix del proyecto activo vía `ProjectManager`
- `top-bar.blade.php`: reemplazado `<a href="#">Cerrar sesión</a>` por `<form method="POST">` con `@csrf`
- El guard correcto lo maneja `BaseAuthController::logout()` sin cambios adicionales

## Archivos clave
- `app/Http/View/Composers/TopbarComposer.php`
- `resources/views/partials/top-bar.blade.php`

## Nota
`#003 — Form edición de perfil` estaba en estado `draft` y no fue completado. Queda fuera del kanban_v2 hasta retomarlo.
