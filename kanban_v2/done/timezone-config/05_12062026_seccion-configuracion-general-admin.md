# 05 — Sección Configuración General en Admin (timezone del tenant)

**Track:** timezone-config
**Proyecto:** landlord
**Prioridad:** medium
**Estado:** done

## Qué se hizo
Campo `timezone` agregado al `SettingsController` y `GeneralSettingsFormRequest` creados por el track `language-config`. Persiste en `tenants.timezone`.
